---
import type {ComponentProps} from "astro/types";
import Trabajo from "./Trabajo.astro";

const trabajos: ComponentProps<typeof Trabajo>[] = [
  {
    title: "The Deeper Underground",
    img: "/public/images/theDeeperUnderground.webp",
    parrafos: [
      `
        Juego de aventura en 3D en <span class="text-secondary font-bold">Unreal Engine</span>.
        Diseño de personajes, escenarios, objetos, animaciones, etc.
        Trabajo en equipo.
      `,
    ]
  },
];

---

<div class="flex flex-col gap-8 p-4">
  {trabajos.map((trabajo) => (
    <Trabajo {...trabajo} />
  ))}
</div>
