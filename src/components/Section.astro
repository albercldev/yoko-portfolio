---
interface Props {
  title: string;
}

const { title }: Props = Astro.props;
---

<section>
  <h1 class="font-handwrite text-3xl text-primary font-bold text-center mb-4">{title}</h1>
  <slot/>
</section>
